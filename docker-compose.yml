version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://Vivek:VSweta%401234@vsproduct.bknr22r.mongodb.net/ats
      - OPENAI_API_KEY=your_openai_key_here
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=vivekananthan2624@gmail.com
      - SMTP_PASSWORD=your_gmail_app_password_here
      - SMTP_FROM=vivekananthan2624@gmail.com
      - SESSION_SECRET=your_secret_key_here
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
    depends_on:
      - mongo
    networks:
      - ats-network

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=admin123
      - MONGO_INITDB_DATABASE=ats
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped
    networks:
      - ats-network

volumes:
  mongo-data:

networks:
  ats-network:
    driver: bridge
